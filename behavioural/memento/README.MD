We use this pattern to implement the undo mecanism in our classes.
We are going to simulate the undo action. The memento pattern is used for that.
Editor == Originator
EditorState == Memento
History == Caretaker

=> We reach 'Single Responsiblity Principle'
